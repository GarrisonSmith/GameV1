namespace Fantasy.Logic.Controllers
{
    /// <summary>
    /// Describes the different contexts a ActionControl can be acting on.
    /// </summary>
    public enum ControlContexts
    {
        current,
        camera,
        character,
        menu
    }
}
