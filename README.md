# Tile String Creation
Converts a tile map.png to a txt file using IDs created from a folder of tile sets.
# Everything else
Logic used for drawing tiles, characters, etc with a moving camera and controls with monogame.
